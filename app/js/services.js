'use strict';

/* Services */

var dotaServices = angular.module('dotaServices', ['ngResource']);

dotaServices.factory('latestMatches', ['$resource', '$http',
    function($resource, $http){


    }]);
